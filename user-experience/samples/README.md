# 用户体验样例字幕

这些文件用于 `user-experience/README.md` 的本地体验命令：

- `good.srt`：干净的基础字幕，用于格式转换和重定时。
- `ai-clean-bad.srt`：带重复词、ASR 标记和标点问题的创作者字幕，用于 `creator clean`。
- `delivery-bad.srt`：带时间轴和阅读体验问题的交付字幕，用于 profile QC。
